edition = "2024";

package actions;

option go_package = "go.goms.io/aks/AKSFlexNode/pkg/actions";

// Metadata represents the metadata of an action to be applied.
message Metadata {
  // type refers to the type of action.
  string type = 1;
  // name refers to the name of the action.
  string name = 2;
}

message Base {
  Metadata metadata = 1;
}