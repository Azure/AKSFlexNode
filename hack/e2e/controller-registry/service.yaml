apiVersion: v1
kind: Service
metadata:
  name: aks-flex-controller-registry
  labels:
    app.kubernetes.io/name: aks-flex-controller-registry
spec:
  selector:
    app.kubernetes.io/name: aks-flex-controller-registry
  ports:
    - name: registry
      port: 5000
      targetPort: registry
