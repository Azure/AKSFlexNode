apiVersion: v1
kind: Service
metadata:
  name: aks-flex-controller
  namespace: kube-system
spec:
  selector:
    app.kubernetes.io/name: aks-flex-controller
  ports:
    - name: http
      port: 80
      targetPort: http
